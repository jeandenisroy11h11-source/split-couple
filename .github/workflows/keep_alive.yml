name: Keep Streamlit Alive

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://split-couple-kc55nz2bgk6fhyhallzftk.streamlit.app/
